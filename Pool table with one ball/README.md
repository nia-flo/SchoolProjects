# Pool table with one ball without friction

This is a simulation of the movement of a ball on a pool table without any friction. The ball has starting coordinates (X and Y), speed and angle (in degrees).